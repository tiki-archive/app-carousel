import 'package:flutter/material.dart';

class InfoCarouselLayout extends StatelessWidget{

  const InfoCarouselLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child:Text('info carousel'));
  }

}